{
  "name": "kundol",
  "version": "0.5.1",
  "license": "MIT",
  "description": "A Bun + TypeScript CLI for optimising developer storage and project build artifacts.",
  "type": "module",
  "private": true,
  "bin": {
    "kundol": "./src/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "register:cli": "bun run scripts/register-cli.ts",
    "test": "bun test",
    "check:tools": "bun run scripts/check-tool-versions.ts",
    "lint": "eslint src tests scripts eslint.config.mjs --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "build:check": "bun build src/cli/index.ts --target=bun --outfile /dev/null",
    "build:release": "bun run scripts/build-release.ts",
    "docs:generate": "bun run scripts/generate-manual.ts",
    "docs:dev": "bun run docs:generate && vitepress dev website",
    "docs:build": "bun run docs:generate && vitepress build website",
    "docs:preview": "vitepress preview website",
    "smoke:cli": "bun run scripts/smoke-cli.ts",
    "registry:new": "bun run scripts/create-registry-entry.ts",
    "registry:check": "bun run scripts/validate-optimisation-registry.ts",
    "registry:programme": "bun run scripts/check-registry-programme.ts",
    "registry:release": "bun run scripts/check-registry-programme.ts --release",
    "check": "bun run check:tools && bun run lint && bun run typecheck && bun run registry:check && bun run registry:programme && bun test && bun run build:check && bun run smoke:cli",
    "prepare": "husky"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.3.5",
    "eslint": "^10.10.0",
    "husky": "^9.1.7",
    "mermaid": "12",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0",
    "vitepress": "1.6.4",
    "vue": "3"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}

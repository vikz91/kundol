import{_ as a,C as o,o as i,c as s,ag as t,E as n}from"./chunks/framework.CAfGEm1x.js";const h=JSON.parse('{"title":"kundol Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}'),l={name:"architecture.md"};function c(d,e,p,u,g,m){const r=o("MermaidDiagram");return i(),s("div",null,[e[0]||(e[0]=t('<h1 id="kundol-architecture" tabindex="-1">kundol Architecture <a class="header-anchor" href="#kundol-architecture" aria-label="Permalink to &quot;kundol Architecture&quot;">​</a></h1><p>Created: 2026-06-13 07:24:10 IST Last updated: 2026-09-17 10:28:16 IST Related tasks: <code>KUN-101</code>, <code>KUN-102</code>, <code>KUN-103</code>, <code>KUN-104</code></p><p>kundol is a Bun + TypeScript CLI. The bundled registry describes cleanup rules; code-owned handlers determine what can execute. See <a href="https://github.com/vikz91/kundol/blob/main/docs/context.md" target="_blank" rel="noreferrer">codebase context</a> for detailed behavior and limits.</p><h2 id="components" tabindex="-1">Components <a class="header-anchor" href="#components" aria-label="Permalink to &quot;Components&quot;">​</a></h2>',4)),n(r,{code:`flowchart TD
    Entry["cli/index.ts"] --> CLI["cli/program.ts · Commander"]
    JSON["registry/optimisations.json"] --> Schema["core/optimisation-registry/schema.ts · Zod"]
    Schema --> CLI
    CLI --> Catalogue["commands/tools.ts · catalogue and requests"]
    CLI --> Routes["commands/optimise.ts"]
    Routes --> Defaults["Resolve supplied / saved scopes; discover Docker if needed"]
    Defaults <--> Settings["SQLite settings · remembered defaults"]
    Defaults --> Actions["cli/actions.ts · scopes, selection, reporting"]
    Actions --> Scope["Project discovery / pinned Docker context"]
    Actions --> Engine["Registry engine · probe / review / apply"]
    Scope --> Engine
    Schema --> Engine
    Engine --> Handlers["Approved selectors, validators and actions"]
    Handlers --> Files["Scoped paths · safe-removal.ts"]
    Handlers --> Owners["Owner-tool commands / resource adapters"]
    Engine --> Sink["Injected audit sink"]
    Actions --> Sink
    Sink --> DB["SQLite actions · ~/.kundol/kundol.db"]
    Sink --> Sessions["Best-effort text logs · ~/.kundol/sessions"]
`}),e[1]||(e[1]=t('<ul><li><strong>CLI:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/cli/program.ts" target="_blank" rel="noreferrer">program.ts</a> wires Commander, registry validation, and injectable dependencies; <a href="https://github.com/vikz91/kundol/blob/main/src/cli/actions.ts" target="_blank" rel="noreferrer">actions.ts</a> coordinates scopes, prints plans, collects selection, and reports results. Catalogue commands read registry data; request/issue commands open GitHub pages.</li><li><strong>Rule contract:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/core/optimisation-registry/schema.ts" target="_blank" rel="noreferrer">schema.ts</a> validates <a href="https://github.com/vikz91/kundol/blob/main/registry/optimisations.json" target="_blank" rel="noreferrer">registry JSON</a>, including scope, lifecycle status, selectors, actions, validators, and review policy.</li><li><strong>Engine:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/engine.ts" target="_blank" rel="noreferrer">engine.ts</a> owns eligibility, bounded concurrent probes, deduplication, review plans, live validation, and sequential execution. Services do not import CLI presentation.</li><li><strong>Handlers:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/local-handler-bindings.ts" target="_blank" rel="noreferrer">local-handler-bindings.ts</a>, <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/path-targets.ts" target="_blank" rel="noreferrer">path-targets.ts</a>, and <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/commands.ts" target="_blank" rel="noreferrer">commands.ts</a> bind rules to approved adapters, generated-path selectors, or owner commands. JSON cannot introduce arbitrary executable behavior.</li><li><strong>Scopes:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/project-roots.ts" target="_blank" rel="noreferrer">project-roots.ts</a> discovers marker-backed roots under the resolved supplied or saved workdir; <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/docker-context-identity.ts" target="_blank" rel="noreferrer">docker-context-identity.ts</a> and <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/docker-pinned-runner.ts" target="_blank" rel="noreferrer">docker-pinned-runner.ts</a> pin Docker operations to a named context and daemon.</li><li><strong>Persistence:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/db/client.ts" target="_blank" rel="noreferrer">db/client.ts</a> and <a href="https://github.com/vikz91/kundol/blob/main/src/db/repositories/action-repository.ts" target="_blank" rel="noreferrer">action-repository.ts</a> store action audits; SQLite settings store canonical project-directory and validated Docker-context defaults; <a href="https://github.com/vikz91/kundol/blob/main/src/services/audit/session-audit-log.ts" target="_blank" rel="noreferrer">session-audit-log.ts</a> writes text sessions. Historical migration tables remain for compatibility, not active workspace indexing.</li><li><strong>Infrastructure:</strong> <code>src/config/</code>, <code>src/platform/</code>, and <code>src/shared/</code> provide paths, home/clock abstractions, output formatting, and exit codes. Tests inject temporary roots, runners, clocks, and database paths.</li></ul><h2 id="execution-flow" tabindex="-1">Execution flow <a class="header-anchor" href="#execution-flow" aria-label="Permalink to &quot;Execution flow&quot;">​</a></h2>',2)),n(r,{code:`sequenceDiagram
    actor User
    participant CLI as CLI actions
    participant Engine as Registry engine
    participant Handler as Approved handlers
    participant Audit as Audit sink
    User->>CLI: optimise scope + options
    CLI->>CLI: Resolve and validate scopes; persist initial or requested defaults
    CLI->>Engine: probe eligible rules
    Engine->>Handler: Discover and validate targets
    Handler-->>Engine: Identities, evidence, sizes / unavailable reasons
    Engine-->>CLI: Probe plan
    CLI-->>User: Print plan
    User->>CLI: Select targets (or force-safe selection)
    CLI->>Engine: review original plan + selection
    Engine-->>CLI: Single-use review plan
    CLI->>Engine: apply review plan
    loop Each selected target, sequentially
        Engine->>Handler: Re-probe identity and validate
        Engine->>Audit: Record attempt before action
        Engine->>Handler: Validate again, execute only if valid
        Engine->>Audit: Record applied / skipped / failed
    end
    Engine-->>CLI: Results and audit warnings
    CLI->>Audit: Record run outcome
    CLI-->>User: Report + exit status
`}),e[2]||(e[2]=t('<ul><li><strong>Routes:</strong> <code>storage</code> uses user scope; <code>projects</code> and <code>repos</code> use workdir scope (<code>repos</code> does not require Git); <code>docker</code> resolves and pins a supplied, saved, or discovered named context. <code>all</code> resolves both scopes and adds system-scope readiness. Initial valid defaults are persisted; later explicit values are temporary unless <code>--save-defaults</code> is supplied. Stale defaults fail without fallback.</li><li><strong>Combined plans:</strong> <code>all</code> probes four scopes concurrently using three engines (system reuses the storage engine), rejects duplicate resources and identical/nested paths, and collects one selection. Original engine plans remain authoritative; scope plans apply sequentially.</li><li><strong>Selection:</strong> Every run prints its plan. <code>-f</code> selects only safe, force-eligible targets; review targets require explicit selection and protected inventory cannot be selected. After scope resolution, no TTY without force records cancellation. Docker discovery with multiple contexts needs a numbered choice or an explicit context, even with force. Standalone Docker has no force option.</li><li><strong>Audits and errors:</strong> Target events reuse one lazily opened SQLite connection per apply run, closed before the summary. Attempt-audit failure prevents that action; outcome-audit failures surface as warnings. Failed target actions produce exit code 70.</li></ul><h2 id="safety-and-capability-boundaries" tabindex="-1">Safety and capability boundaries <a class="header-anchor" href="#safety-and-capability-boundaries" aria-label="Permalink to &quot;Safety and capability boundaries&quot;">​</a></h2><ul><li><strong>Release gates:</strong> Status, scope, platform, handler support, and safety policy must match. <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/beta-execution.ts" target="_blank" rel="noreferrer">beta-execution.ts</a> permits five exact opt-in beta IDs: Yarn cache review, two Linux Python review rules, and two protected Docker inventories.</li><li><strong>Live checks:</strong> Apply accepts only its engine&#39;s issued, unused review plan and rechecks target identity and applicable validators. Generated-path activity checks precede sizing: activity is bounded at 150,000 entries; sizing at 50,000 (incomplete size is unknown).</li><li><strong>Removal:</strong> <a href="https://github.com/vikz91/kundol/blob/main/src/services/optimisation-registry/safe-removal.ts" target="_blank" rel="noreferrer">safe-removal.ts</a> uses an isolated system-Python helper with no-follow, descriptor-relative removal and fails closed if unsupported. The remaining final leaf-name replacement race is documented in <a href="https://github.com/vikz91/kundol/blob/main/docs/context.md" target="_blank" rel="noreferrer">context</a>.</li><li><strong>Current limits:</strong> No Docker cleanup rule is published; beta network/volume inventory never removes resources. Staged adapters do not imply public availability. Broad Docker prune, arbitrary temp cleanup, startup changes, archives, a daemon, and a dashboard are outside the current CLI.</li></ul><p>See <a href="/kundol/usage.html">usage</a> for commands and the <a href="/kundol/registry.html">registry guide</a> for extension/publication gates. Verification lives in <a href="https://github.com/vikz91/kundol/blob/main/tests/cli/" target="_blank" rel="noreferrer">CLI tests</a>, <a href="https://github.com/vikz91/kundol/blob/main/tests/services/" target="_blank" rel="noreferrer">engine and adapter tests</a>, and <a href="https://github.com/vikz91/kundol/blob/main/tests/docker-acceptance/README.md" target="_blank" rel="noreferrer">isolated Docker acceptance tests</a>.</p>',4))])}const f=a(l,[["render",c]]);export{h as __pageData,f as default};
